Drop the resume PDF in this folder named exactly:

    Stephen-Morgan-Resume.pdf

That is the filename the "Resume" button in `index.html` links to. Until the
file exists, that button 404s — either add the PDF or remove the button from
the contact section.
